Flask==2.2.5          # 最新 2.x，仍與 Flask 3 同 API 但保留 url_quote
Werkzeug==2.3.7       # <3，避免 ImportError
Flask-Cors==3.0.10
gunicorn==20.1.0
google-cloud-aiplatform==1.18.1
google-auth==2.11.0
protobuf>=3.19.0,<5.0.0dev

